# 숫자형 벡터 처리 함수

# 1. abs() 절대값 absolut value

abs(10)
abs(-10)

# 2. sqrt() 제곱근

sqrt(16)
sqrt(-16) # error

# 3. ceiling() : 실수 x 보다 큰 값들중 가장 작은 정수

ceiling(5.88)
ceiling(6.00)

# 4. floor() : 실수 x 보다 작은 값들중 가장 큰 정수

floor(5.88)
floor(5.00)

# 5. trunc() : 소수점 이하는 잘라서 버려버림

trunc(5.88)
trunc(5.00)

# 6. 